//confirm if this is what is returned in both  cases
export interface ProductData {
  item_title: string;
  id: number | string;
  item_image_url: string;
  requested_price: string;
  last_price: string;
}
